
interface Bar {
  fraction: number,
  textLeft: string,
  textRight: string,

  elements: {
    outer: HTMLElement,
    inner: HTMLElement,
    textRight: HTMLElement,
    textLeft: HTMLElement
  }
}